---
permalink: /404.html
---
{% import "eleventy.partials.njk" as ui %}
<!doctype html>
<html lang="ru">
  <head>
    <meta charset="UTF-8" />
    <!-- Yandex.Metrika counter -->
    <script type="text/javascript">
      (function (m, e, t, r, i, k, a) {
        m[i] = m[i] || function () { (m[i].a = m[i].a || []).push(arguments); };
        m[i].l = 1 * new Date();
        for (var j = 0; j < document.scripts.length; j += 1) {
          if (document.scripts[j].src === r) { return; }
        }
        k = e.createElement(t);
        a = e.getElementsByTagName(t)[0];
        k.async = 1;
        k.src = r;
        a.parentNode.insertBefore(k, a);
      })(window, document, 'script', 'https://mc.yandex.ru/metrika/tag.js?id=108660099', 'ym');

      ym(108660099, 'init', {
        ssr: true,
        webvisor: true,
        clickmap: true,
        ecommerce: 'dataLayer',
        referrer: document.referrer,
        url: location.href,
        accurateTrackBounce: true,
        trackLinks: true
      });
    </script>
    <!-- /Yandex.Metrika counter -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
    <meta name="robots" content="noindex,nofollow" />
    <title>Страница не найдена | Vetor Studio</title>
    <style>
      :root { color-scheme: dark; font-family: Inter, "Segoe UI", sans-serif; }
      body { margin: 0; min-height: 100vh; display: grid; place-items: center; background: radial-gradient(circle at 20% 20%, rgba(154,233,35,.12), transparent 40%), linear-gradient(145deg, #09031c, #11043a); color: #f5f7fb; }
      .nf-shell { width: min(980px, calc(100% - 40px)); display: grid; grid-template-columns: minmax(0, 1fr) minmax(180px, .7fr); gap: 24px; padding: 32px; border-radius: 28px; border: 1px solid rgba(255,255,255,.1); background: rgba(14,16,26,.82); box-shadow: 0 30px 90px rgba(0,0,0,.42); }
      .nf-title-block h1 { margin: 0; font-size: clamp(34px, 7vw, 60px); line-height: 1.02; letter-spacing: -.04em; }
      .nf-title-block p { margin: 16px 0 0; color: #a8afbc; font-size: 17px; line-height: 1.5; }
      .nf-eyebrow { margin: 0 0 14px; font-size: 12px; font-weight: 800; letter-spacing: .12em; text-transform: uppercase; color: #f1ff1a; }
      .nf-button { display: inline-flex; align-items: center; justify-content: center; margin-top: 24px; min-height: 46px; padding: 0 18px; border-radius: 14px; border: 1px solid rgba(255,255,255,.18); background: rgba(255,255,255,.03); color: #f5f7fb; text-decoration: none; font-weight: 800; }
      .nf-preview { display: grid; place-items: center; border-radius: 22px; border: 1px solid rgba(255,255,255,.1); background: rgba(255,255,255,.03); font-size: clamp(48px, 9vw, 82px); font-weight: 900; color: rgba(245,247,251,.4); }
      @media (max-width: 760px) { .nf-shell { grid-template-columns: 1fr; } .nf-preview { min-height: 180px; } }
    </style>
  </head>
  <body>
    <noscript><div><img src="https://mc.yandex.ru/watch/108660099" style="position:absolute; left:-9999px;" alt="" /></div></noscript>
    <main class="nf-shell">
      <section>
        {{ ui.titleBlock("Раздел недоступен", "Страница не найдена", "Ссылка устарела, раздел скрыт или страница ещё не опубликована.") }}
        {{ ui.ctaButton("Вернуться на главную", "/") }}
      </section>
      <section class="nf-preview" aria-hidden="true">404</section>
    </main>
  </body>
</html>
